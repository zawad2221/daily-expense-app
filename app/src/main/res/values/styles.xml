<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/alterPrimary</item>
        <item name="colorPrimaryDark">@color/alterPrimaryDark</item>
        <item name="colorAccent">@color/alterAccent</item>
        <item name="android:fontFamily">@font/noto_sans</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="SettingsScreen" parent="AppTheme">
        <item name="colorAccent">@color/alterPrimaryDark</item>

    </style>

</resources>